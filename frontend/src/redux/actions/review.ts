import { Dispatch } from "@reduxjs/toolkit";
import axios from "axios";
// post review
export const postReview = (details: any)=> async(dispatch: Dispatch, getState: Function)=> {
    try{

    } catch(error){

    }
}

// deleting review
export const removeReview = (id: number) => async(dispatch: Dispatch, getState: Function)=> {
    try{

    } catch(error){

    }
}