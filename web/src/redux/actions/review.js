import { Dispatch } from "@reduxjs/toolkit";
import axios from "axios";
// post review
export const postReview = (details)=> async(dispatch, getState)=> {
    try{

    } catch(error){

    }
}

// deleting review
export const removeReview = (id) => async(dispatch, getState)=> {
    try{

    } catch(error){

    }
}